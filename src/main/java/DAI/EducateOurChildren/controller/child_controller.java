package DAI.EducateOurChildren.controller;
/*
import DAI.EducateOurChildren.model.child;

import DAI.EducateOurChildren.service.child_service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping(path = "/api")
public class child_controller {

    private final child_service ChildService;

    @Autowired
    public child_controller(child_service childService) {
        ChildService = childService;
    }

    @GetMapping
    public List<child> getChild(){
        return child_service.getChild();
    }

    @PostMapping
    public void registerNewChild(@RequestBody child child){
        child_service.addNewChild(child);
    }

    @DeleteMapping(path = "{id_child}")
    public void deleteChild(@PathVariable("id_child") Integer id_child){
        child_service.deleteChild(id_child);
    }
}*/


